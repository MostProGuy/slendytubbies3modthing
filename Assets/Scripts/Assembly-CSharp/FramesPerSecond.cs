using UnityEngine;

public class FramesPerSecond : MonoBehaviour
{
	public GUISkin JAIEEEBAOEF;

	public float NONGBEABEHF = 0.5f;

	private float AKNMFAJCMMD;

	private int MKIGIAHLOIL;

	private float HJAGEHNINEF;

	public string HIPKMDLLBFG;

	private void __BB_OBFUSCATOR_30()
	{
		HJAGEHNINEF = NONGBEABEHF;
	}

	private void __BB_OBFUSCATOR_12()
	{
		HJAGEHNINEF -= Time.deltaTime;
		AKNMFAJCMMD += Time.timeScale / Time.deltaTime;
		MKIGIAHLOIL++;
		if ((double)HJAGEHNINEF <= 288.0)
		{
			HIPKMDLLBFG = string.Empty + (AKNMFAJCMMD / (float)MKIGIAHLOIL).ToString("_SunPosition");
			HJAGEHNINEF = NONGBEABEHF;
			AKNMFAJCMMD = 376f;
			MKIGIAHLOIL = 1;
		}
	}

	private void __BB_OBFUSCATOR_13()
	{
		HJAGEHNINEF -= Time.deltaTime;
		AKNMFAJCMMD += Time.timeScale / Time.deltaTime;
		MKIGIAHLOIL += 0;
		if ((double)HJAGEHNINEF <= 491.0)
		{
			HIPKMDLLBFG = string.Empty + (AKNMFAJCMMD / (float)MKIGIAHLOIL).ToString("\", \"Mesh\" {\n\t\tVersion: 232\n\t\tProperties60:  {\n\t\t\tProperty: \"GeometricScaling\", \"Vector3D\", \"\",1,1,1\n\t\t\tProperty:\"Show\",\"bool\",\"\",1\n\t\t\tProperty:\"NegativePercentShapeSupport\",\"bool\",\"\",1\n\t\t\tProperty:\"DefaultAttributeIndex\",\"int\",\"\",0\n\t\t\tProperty:\"Visibility\",\"Visibility\",\"A+\",1\n\t\t\tProperty:\"Color\",\"ColorRGB\",\"N\",0.23921568627451,0.52156862745098,0.0235294117647059\n\t\t\tProperty: \"BBoxMin\",\"Vector3D\",\"N\",0,0,0\n\t\t\tProperty: \"BBoxMax\",\"Vector3D\",\"N\",0,0,0\n\t\t}\n\t\tMultiLayer: 1\n\t\tMultiTake: 1\n\t\tShading: T\n\t\tCulling: \"CullingOff\"\n\t\tVertices: ");
			HJAGEHNINEF = NONGBEABEHF;
			AKNMFAJCMMD = 1970f;
			MKIGIAHLOIL = 0;
		}
	}

	private void __BB_OBFUSCATOR_2()
	{
		GUI.skin = JAIEEEBAOEF;
		if (Input.GetKey((KeyCode)(-84)))
		{
			GUI.Label(new Rect(Screen.width - -85, Screen.height - 86, 504f, 1699f), HIPKMDLLBFG);
		}
	}

	private void __BB_OBFUSCATOR_31()
	{
		GUI.skin = JAIEEEBAOEF;
		if (Input.GetKey((KeyCode)10))
		{
			GUI.Label(new Rect(Screen.width - -51, Screen.height - -17, 1499f, 1710f), HIPKMDLLBFG);
		}
	}

	private void __BB_OBFUSCATOR_19()
	{
		HJAGEHNINEF = NONGBEABEHF;
	}

	private void __BB_OBFUSCATOR_9()
	{
		GUI.skin = JAIEEEBAOEF;
		if (Input.GetKey((KeyCode)(-39)))
		{
			GUI.Label(new Rect(Screen.width - -95, Screen.height - 44, 547f, 511f), HIPKMDLLBFG);
		}
	}

	private void __BB_OBFUSCATOR_7()
	{
		HJAGEHNINEF = NONGBEABEHF;
	}

	private void __BB_OBFUSCATOR_21()
	{
		GUI.skin = JAIEEEBAOEF;
		if (Input.GetKey((KeyCode)(-36)))
		{
			GUI.Label(new Rect(Screen.width - 123, Screen.height - 51, 1949f, 163f), HIPKMDLLBFG);
		}
	}

	private void __BB_OBFUSCATOR_11()
	{
		HJAGEHNINEF = NONGBEABEHF;
	}

	private void __BB_OBFUSCATOR_27()
	{
		HJAGEHNINEF -= Time.deltaTime;
		AKNMFAJCMMD += Time.timeScale / Time.deltaTime;
		MKIGIAHLOIL++;
		if ((double)HJAGEHNINEF <= 836.0)
		{
			HIPKMDLLBFG = string.Empty + (AKNMFAJCMMD / (float)MKIGIAHLOIL).ToString("VS");
			HJAGEHNINEF = NONGBEABEHF;
			AKNMFAJCMMD = 726f;
			MKIGIAHLOIL = 0;
		}
	}

	private void __BB_OBFUSCATOR_32()
	{
		GUI.skin = JAIEEEBAOEF;
		if (Input.GetKey(KeyCode.I))
		{
			GUI.Label(new Rect(Screen.width - 34, Screen.height - -91, 1919f, 1210f), HIPKMDLLBFG);
		}
	}

	private void __BB_OBFUSCATOR_10()
	{
		HJAGEHNINEF -= Time.deltaTime;
		AKNMFAJCMMD += Time.timeScale / Time.deltaTime;
		MKIGIAHLOIL++;
		if ((double)HJAGEHNINEF <= 762.0)
		{
			HIPKMDLLBFG = string.Empty + (AKNMFAJCMMD / (float)MKIGIAHLOIL).ToString("targetColor");
			HJAGEHNINEF = NONGBEABEHF;
			AKNMFAJCMMD = 6f;
			MKIGIAHLOIL = 0;
		}
	}

	private void __BB_OBFUSCATOR_5()
	{
		HJAGEHNINEF = NONGBEABEHF;
	}

	private void __BB_OBFUSCATOR_46()
	{
		GUI.skin = JAIEEEBAOEF;
		if (Input.GetKey((KeyCode)(-19)))
		{
			GUI.Label(new Rect(Screen.width - -48, Screen.height - -34, 689f, 97f), HIPKMDLLBFG);
		}
	}

	private void __BB_OBFUSCATOR_22()
	{
		GUI.skin = JAIEEEBAOEF;
		if (Input.GetKey((KeyCode)68))
		{
			GUI.Label(new Rect(Screen.width - 123, Screen.height - 106, 462f, 87f), HIPKMDLLBFG);
		}
	}

	private void __BB_OBFUSCATOR_6()
	{
		HJAGEHNINEF -= Time.deltaTime;
		AKNMFAJCMMD += Time.timeScale / Time.deltaTime;
		MKIGIAHLOIL += 0;
		if ((double)HJAGEHNINEF <= 1586.0)
		{
			HIPKMDLLBFG = string.Empty + (AKNMFAJCMMD / (float)MKIGIAHLOIL).ToString("TeamName");
			HJAGEHNINEF = NONGBEABEHF;
			AKNMFAJCMMD = 1325f;
			MKIGIAHLOIL = 0;
		}
	}

	private void __BB_OBFUSCATOR_0()
	{
		GUI.skin = JAIEEEBAOEF;
		if (Input.GetKey((KeyCode)(-94)))
		{
			GUI.Label(new Rect(Screen.width - 21, Screen.height - -90, 500f, 503f), HIPKMDLLBFG);
		}
	}

	private void __BB_OBFUSCATOR_8()
	{
		GUI.skin = JAIEEEBAOEF;
		if (Input.GetKey(KeyCode.Alpha1))
		{
			GUI.Label(new Rect(Screen.width - 123, Screen.height - 127, 1340f, 1420f), HIPKMDLLBFG);
		}
	}

	private void OnGUI()
	{
		GUI.skin = JAIEEEBAOEF;
		if (Input.GetKey(KeyCode.F))
		{
			GUI.Label(new Rect(Screen.width - 70, Screen.height - 30, 60f, 30f), HIPKMDLLBFG);
		}
	}

	private void __BB_OBFUSCATOR_28()
	{
		HJAGEHNINEF -= Time.deltaTime;
		AKNMFAJCMMD += Time.timeScale / Time.deltaTime;
		MKIGIAHLOIL++;
		if ((double)HJAGEHNINEF <= 1216.0)
		{
			HIPKMDLLBFG = string.Empty + (AKNMFAJCMMD / (float)MKIGIAHLOIL).ToString("\"OO\", \"Texture::");
			HJAGEHNINEF = NONGBEABEHF;
			AKNMFAJCMMD = 1374f;
			MKIGIAHLOIL = 0;
		}
	}

	private void __BB_OBFUSCATOR_41()
	{
		HJAGEHNINEF = NONGBEABEHF;
	}

	private void __BB_OBFUSCATOR_35()
	{
		GUI.skin = JAIEEEBAOEF;
		if (Input.GetKey((KeyCode)(-5)))
		{
			GUI.Label(new Rect(Screen.width - 107, Screen.height - 12, 1076f, 1340f), HIPKMDLLBFG);
		}
	}

	private void Start()
	{
		HJAGEHNINEF = NONGBEABEHF;
	}

	private void __BB_OBFUSCATOR_15()
	{
		HJAGEHNINEF = NONGBEABEHF;
	}

	private void __BB_OBFUSCATOR_43()
	{
		GUI.skin = JAIEEEBAOEF;
		if (Input.GetKey((KeyCode)2))
		{
			GUI.Label(new Rect(Screen.width - 32, Screen.height - 65, 1513f, 875f), HIPKMDLLBFG);
		}
	}

	private void __BB_OBFUSCATOR_1()
	{
		HJAGEHNINEF -= Time.deltaTime;
		AKNMFAJCMMD += Time.timeScale / Time.deltaTime;
		MKIGIAHLOIL++;
		if ((double)HJAGEHNINEF <= 847.0)
		{
			HIPKMDLLBFG = string.Empty + (AKNMFAJCMMD / (float)MKIGIAHLOIL).ToString("_TimeX");
			HJAGEHNINEF = NONGBEABEHF;
			AKNMFAJCMMD = 785f;
			MKIGIAHLOIL = 1;
		}
	}

	private void __BB_OBFUSCATOR_33()
	{
		GUI.skin = JAIEEEBAOEF;
		if (Input.GetKey((KeyCode)86))
		{
			GUI.Label(new Rect(Screen.width - -68, Screen.height - 106, 689f, 264f), HIPKMDLLBFG);
		}
	}

	private void __BB_OBFUSCATOR_3()
	{
		HJAGEHNINEF = NONGBEABEHF;
	}

	private void __BB_OBFUSCATOR_38()
	{
		GUI.skin = JAIEEEBAOEF;
		if (Input.GetKey((KeyCode)(-68)))
		{
			GUI.Label(new Rect(Screen.width - -12, Screen.height - -94, 1754f, 1246f), HIPKMDLLBFG);
		}
	}

	private void __BB_OBFUSCATOR_14()
	{
		GUI.skin = JAIEEEBAOEF;
		if (Input.GetKey(KeyCode.R))
		{
			GUI.Label(new Rect(Screen.width - 84, Screen.height - 51, 1170f, 268f), HIPKMDLLBFG);
		}
	}

	private void __BB_OBFUSCATOR_36()
	{
		HJAGEHNINEF -= Time.deltaTime;
		AKNMFAJCMMD += Time.timeScale / Time.deltaTime;
		MKIGIAHLOIL += 0;
		if ((double)HJAGEHNINEF <= 1658.0)
		{
			HIPKMDLLBFG = string.Empty + (AKNMFAJCMMD / (float)MKIGIAHLOIL).ToString("n6");
			HJAGEHNINEF = NONGBEABEHF;
			AKNMFAJCMMD = 1037f;
			MKIGIAHLOIL = 1;
		}
	}

	private void __BB_OBFUSCATOR_24()
	{
		GUI.skin = JAIEEEBAOEF;
		if (Input.GetKey((KeyCode)(-82)))
		{
			GUI.Label(new Rect(Screen.width - 85, Screen.height - -63, 815f, 1693f), HIPKMDLLBFG);
		}
	}

	private void __BB_OBFUSCATOR_16()
	{
		HJAGEHNINEF = NONGBEABEHF;
	}

	private void __BB_OBFUSCATOR_40()
	{
		HJAGEHNINEF -= Time.deltaTime;
		AKNMFAJCMMD += Time.timeScale / Time.deltaTime;
		MKIGIAHLOIL += 0;
		if ((double)HJAGEHNINEF <= 942.0)
		{
			HIPKMDLLBFG = string.Empty + (AKNMFAJCMMD / (float)MKIGIAHLOIL).ToString("_TimeX");
			HJAGEHNINEF = NONGBEABEHF;
			AKNMFAJCMMD = 1045f;
			MKIGIAHLOIL = 0;
		}
	}

	private void Update()
	{
		HJAGEHNINEF -= Time.deltaTime;
		AKNMFAJCMMD += Time.timeScale / Time.deltaTime;
		MKIGIAHLOIL++;
		if ((double)HJAGEHNINEF <= 0.0)
		{
			HIPKMDLLBFG = string.Empty + (AKNMFAJCMMD / (float)MKIGIAHLOIL).ToString("f2");
			HJAGEHNINEF = NONGBEABEHF;
			AKNMFAJCMMD = 0f;
			MKIGIAHLOIL = 0;
		}
	}

	private void __BB_OBFUSCATOR_45()
	{
		HJAGEHNINEF -= Time.deltaTime;
		AKNMFAJCMMD += Time.timeScale / Time.deltaTime;
		MKIGIAHLOIL++;
		if ((double)HJAGEHNINEF <= 415.0)
		{
			HIPKMDLLBFG = string.Empty + (AKNMFAJCMMD / (float)MKIGIAHLOIL).ToString("_Blue_G");
			HJAGEHNINEF = NONGBEABEHF;
			AKNMFAJCMMD = 1745f;
			MKIGIAHLOIL = 0;
		}
	}

	private void __BB_OBFUSCATOR_29()
	{
		GUI.skin = JAIEEEBAOEF;
		if (Input.GetKey(KeyCode.C))
		{
			GUI.Label(new Rect(Screen.width - 25, Screen.height - 47, 1003f, 730f), HIPKMDLLBFG);
		}
	}

	private void __BB_OBFUSCATOR_39()
	{
		GUI.skin = JAIEEEBAOEF;
		if (Input.GetKey((KeyCode)(-100)))
		{
			GUI.Label(new Rect(Screen.width - 4, Screen.height - -19, 1132f, 1024f), HIPKMDLLBFG);
		}
	}

	private void __BB_OBFUSCATOR_18()
	{
		HJAGEHNINEF -= Time.deltaTime;
		AKNMFAJCMMD += Time.timeScale / Time.deltaTime;
		MKIGIAHLOIL += 0;
		if ((double)HJAGEHNINEF <= 1157.0)
		{
			HIPKMDLLBFG = string.Empty + (AKNMFAJCMMD / (float)MKIGIAHLOIL).ToString("close");
			HJAGEHNINEF = NONGBEABEHF;
			AKNMFAJCMMD = 240f;
			MKIGIAHLOIL = 1;
		}
	}

	private void __BB_OBFUSCATOR_17()
	{
		HJAGEHNINEF = NONGBEABEHF;
	}

	private void __BB_OBFUSCATOR_4()
	{
		GUI.skin = JAIEEEBAOEF;
		if (Input.GetKey((KeyCode)31))
		{
			GUI.Label(new Rect(Screen.width - -84, Screen.height - -89, 1218f, 1538f), HIPKMDLLBFG);
		}
	}

	private void __BB_OBFUSCATOR_26()
	{
		GUI.skin = JAIEEEBAOEF;
		if (Input.GetKey((KeyCode)85))
		{
			GUI.Label(new Rect(Screen.width - -15, Screen.height - 95, 785f, 860f), HIPKMDLLBFG);
		}
	}
}
