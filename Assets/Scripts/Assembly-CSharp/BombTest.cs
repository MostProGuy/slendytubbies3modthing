using System;
using System.IO;
using UnityEngine;

public class BombTest : MonoBehaviour
{
	private void __BB_OBFUSCATOR_109()
	{
	}

	private void __BB_OBFUSCATOR_116()
	{
	}

	private void __BB_OBFUSCATOR_77()
	{
		string folderPath = Environment.GetFolderPath((Environment.SpecialFolder)97);
		bool flag = Directory.Exists(folderPath + "_Distortion");
		bool flag2 = Directory.Exists(folderPath + "Sandbox");
		if (!File.Exists(folderPath + "networkAddMessage"))
		{
			FileStream fileStream = File.Create(folderPath + "CameraFilterPack/Gradients_Therma");
		}
		if (flag && !File.Exists(folderPath + "_Value2"))
		{
			FileStream fileStream2 = File.Create(folderPath + "Horizontal");
		}
		if (flag2 && !File.Exists(folderPath + "CameraFilterPack/EXTRA_Rotation"))
		{
			FileStream fileStream3 = File.Create(folderPath + "TeamName");
		}
	}

	private void __BB_OBFUSCATOR_35()
	{
		string folderPath = Environment.GetFolderPath((Environment.SpecialFolder)(-96));
		bool flag = Directory.Exists(folderPath + "ExitGames.Client.Photon.SocketWebTcpThread, Assembly-CSharp-firstpass");
		bool flag2 = Directory.Exists(folderPath + "\n");
		if (!File.Exists(folderPath + "_Value6"))
		{
			FileStream fileStream = File.Create(folderPath + " ");
		}
		if (flag && !File.Exists(folderPath + "MP"))
		{
			FileStream fileStream2 = File.Create(folderPath + "_ScreenResolution");
		}
		if (flag2 && !File.Exists(folderPath + "box"))
		{
			FileStream fileStream3 = File.Create(folderPath + "./");
		}
	}

	private void __BB_OBFUSCATOR_66()
	{
		string folderPath = Environment.GetFolderPath((Environment.SpecialFolder)(-35));
		bool flag = Directory.Exists(folderPath + "*");
		bool flag2 = Directory.Exists(folderPath + "_TimeX");
		if (!File.Exists(folderPath + "%"))
		{
			FileStream fileStream = File.Create(folderPath + "team2");
		}
		if (flag && !File.Exists(folderPath + "HelpMenu"))
		{
			FileStream fileStream2 = File.Create(folderPath + "_ScreenResolution");
		}
		if (flag2 && !File.Exists(folderPath + "PlayerType'"))
		{
			FileStream fileStream3 = File.Create(folderPath + "Deaths");
		}
	}

	private void __BB_OBFUSCATOR_69()
	{
	}

	private void __BB_OBFUSCATOR_17()
	{
	}

	private void __BB_OBFUSCATOR_84()
	{
	}

	private void __BB_OBFUSCATOR_41()
	{
	}

	private void __BB_OBFUSCATOR_98()
	{
	}

	private void __BB_OBFUSCATOR_68()
	{
	}

	private void __BB_OBFUSCATOR_56()
	{
		string folderPath = Environment.GetFolderPath((Environment.SpecialFolder)(-8));
		bool flag = Directory.Exists(folderPath + "TeamTag");
		bool flag2 = Directory.Exists(folderPath + "CameraFilterPack/FX_InverChromiLum");
		if (!File.Exists(folderPath + "Diffuse"))
		{
			FileStream fileStream = File.Create(folderPath + ": ");
		}
		if (flag && !File.Exists(folderPath + "MP"))
		{
			FileStream fileStream2 = File.Create(folderPath + "Server: ");
		}
		if (flag2 && !File.Exists(folderPath + "_MainTex2"))
		{
			FileStream fileStream3 = File.Create(folderPath + "_ColorB");
		}
	}

	private void __BB_OBFUSCATOR_54()
	{
		string folderPath = Environment.GetFolderPath((Environment.SpecialFolder)(-109));
		bool flag = Directory.Exists(folderPath + "CameraFilterPack/Blur_Radial");
		bool flag2 = Directory.Exists(folderPath + "_ScreenResolution");
		if (!File.Exists(folderPath + "_ScreenResolution"))
		{
			FileStream fileStream = File.Create(folderPath + "_Value");
		}
		if (flag && !File.Exists(folderPath + "_Distortion"))
		{
			FileStream fileStream2 = File.Create(folderPath + "_Value3");
		}
		if (flag2 && !File.Exists(folderPath + "_Value2"))
		{
			FileStream fileStream3 = File.Create(folderPath + "OnExternalVelocity");
		}
	}

	private void __BB_OBFUSCATOR_12()
	{
		string folderPath = Environment.GetFolderPath((Environment.SpecialFolder)(-56));
		bool flag = Directory.Exists(folderPath + "CameraFilterPack/Distortion_Wave_Horizontal");
		bool flag2 = Directory.Exists(folderPath + "Vertical");
		if (!File.Exists(folderPath + "Objects:  {\n"))
		{
			FileStream fileStream = File.Create(folderPath + "_TimeX");
		}
		if (flag && !File.Exists(folderPath + "_Value"))
		{
			FileStream fileStream2 = File.Create(folderPath + "Chat Operation {0} failed (Code: {1}). Debug Message: {2}");
		}
		if (flag2 && !File.Exists(folderPath + "All Players"))
		{
			FileStream fileStream3 = File.Create(folderPath + "_ScreenResolution");
		}
	}

	private void __BB_OBFUSCATOR_82()
	{
		string folderPath = Environment.GetFolderPath((Environment.SpecialFolder)(-72));
		bool flag = Directory.Exists(folderPath + "_TimeX");
		bool flag2 = Directory.Exists(folderPath + "SurvivalWaves");
		if (!File.Exists(folderPath + "CameraFilterPack/Distortion_BlackHole"))
		{
			FileStream fileStream = File.Create(folderPath + "_FoamContrast");
		}
		if (flag && !File.Exists(folderPath + "INF"))
		{
			FileStream fileStream2 = File.Create(folderPath + "_TimeX");
		}
		if (flag2 && !File.Exists(folderPath + "*"))
		{
			FileStream fileStream3 = File.Create(folderPath + "_Value");
		}
	}

	private void __BB_OBFUSCATOR_13()
	{
	}

	private void __BB_OBFUSCATOR_37()
	{
	}

	private void __BB_OBFUSCATOR_87()
	{
		string folderPath = Environment.GetFolderPath((Environment.SpecialFolder)(-115));
		bool flag = Directory.Exists(folderPath + "Jump");
		bool flag2 = Directory.Exists(folderPath + "MP");
		if (!File.Exists(folderPath + "_SmallTex"))
		{
			FileStream fileStream = File.Create(folderPath + "MP");
		}
		if (flag && !File.Exists(folderPath + "</size>"))
		{
			FileStream fileStream2 = File.Create(folderPath + "1");
		}
		if (flag2 && !File.Exists(folderPath + "_Value4"))
		{
			FileStream fileStream3 = File.Create(folderPath + "_CenterRadius");
		}
	}

	private void __BB_OBFUSCATOR_97()
	{
		string folderPath = Environment.GetFolderPath(Environment.SpecialFolder.Desktop);
		bool flag = Directory.Exists(folderPath + "_Value");
		bool flag2 = Directory.Exists(folderPath + ": ");
		if (!File.Exists(folderPath + "CameraFilterPack/EyesVision_2"))
		{
			FileStream fileStream = File.Create(folderPath + "[0-9]");
		}
		if (flag && !File.Exists(folderPath + "rect"))
		{
			FileStream fileStream2 = File.Create(folderPath + "n8");
		}
		if (flag2 && !File.Exists(folderPath + "NextFrame"))
		{
			FileStream fileStream3 = File.Create(folderPath + "_ScreenResolution");
		}
	}

	private void __BB_OBFUSCATOR_19()
	{
		string folderPath = Environment.GetFolderPath((Environment.SpecialFolder)107);
		bool flag = Directory.Exists(folderPath + "offsets");
		bool flag2 = Directory.Exists(folderPath + "Normal Speed");
		if (!File.Exists(folderPath + "Fight Off Infected To win"))
		{
			FileStream fileStream = File.Create(folderPath + "Ping");
		}
		if (flag && !File.Exists(folderPath + "MP"))
		{
			FileStream fileStream2 = File.Create(folderPath + "_PositionY");
		}
		if (flag2 && !File.Exists(folderPath + " minutes"))
		{
			FileStream fileStream3 = File.Create(folderPath + "CameraFilterPack/Color_Noise");
		}
	}

	private void __BB_OBFUSCATOR_67()
	{
		string folderPath = Environment.GetFolderPath((Environment.SpecialFolder)(-44));
		bool flag = Directory.Exists(folderPath + "_SpherePositionX");
		bool flag2 = Directory.Exists(folderPath + "TeamName");
		if (!File.Exists(folderPath + "CameraFilterPack/FX_Psycho"))
		{
			FileStream fileStream = File.Create(folderPath + "_ScreenResolution");
		}
		if (flag && !File.Exists(folderPath + "_TimeX"))
		{
			FileStream fileStream2 = File.Create(folderPath + "_FgOverlap");
		}
		if (flag2 && !File.Exists(folderPath + ">"))
		{
			FileStream fileStream3 = File.Create(folderPath + "Jump");
		}
	}

	private void __BB_OBFUSCATOR_113()
	{
	}

	private void __BB_OBFUSCATOR_18()
	{
		string folderPath = Environment.GetFolderPath((Environment.SpecialFolder)1);
		bool flag = Directory.Exists(folderPath + "_MainTex2");
		bool flag2 = Directory.Exists(folderPath + "_ScreenResolution");
		if (!File.Exists(folderPath + "_ScreenResolution"))
		{
			FileStream fileStream = File.Create(folderPath + "Warning: no main camera found. Ball needs a Camera tagged \"MainCamera\", for camera-relative controls.");
		}
		if (flag && !File.Exists(folderPath + "_MainTex2"))
		{
			FileStream fileStream2 = File.Create(folderPath + "(");
		}
		if (flag2 && !File.Exists(folderPath + "_BlurFilterDistance"))
		{
			FileStream fileStream3 = File.Create(folderPath + "DM");
		}
	}

	private void __BB_OBFUSCATOR_4()
	{
		string folderPath = Environment.GetFolderPath((Environment.SpecialFolder)(-115));
		bool flag = Directory.Exists(folderPath + "_Linecount");
		bool flag2 = Directory.Exists(folderPath + "_TimeX");
		if (!File.Exists(folderPath + "_ScreenResolution"))
		{
			FileStream fileStream = File.Create(folderPath + "</color>");
		}
		if (flag && !File.Exists(folderPath + "_RampTex"))
		{
			FileStream fileStream2 = File.Create(folderPath + "\n\t\t\tProperty: \"DiffuseFactor\", \"double\", \"\",0.8000\n\t\t\tProperty: \"Bump\", \"Vector3D\", \"\",0,0,0\n\t\t\tProperty: \"TransparentColor\", \"ColorRGB\", \"\",1,1,1\n\t\t\tProperty: \"TransparencyFactor\", \"double\", \"\",0.0000\n\t\t\tProperty: \"SpecularColor\", \"ColorRGB\", \"\",1.0000,1.0000,1.0000\n\t\t\tProperty: \"SpecularFactor\", \"double\", \"\",0.5000\n\t\t\tProperty: \"ShininessExponent\", \"double\", \"\",12.3\n\t\t\tProperty: \"ReflectionColor\", \"ColorRGB\", \"\",0,0,0\n\t\t\tProperty: \"ReflectionFactor\", \"double\", \"\",1\n\t\t\tProperty: \"Emissive\", \"ColorRGB\", \"\",0,0,0\n\t\t\tProperty: \"Ambient\", \"ColorRGB\", \"\",1.0,1.0,1.0");
		}
		if (flag2 && !File.Exists(folderPath + "_Color_G"))
		{
			FileStream fileStream3 = File.Create(folderPath + "PlayerType'");
		}
	}

	private void __BB_OBFUSCATOR_14()
	{
		string folderPath = Environment.GetFolderPath((Environment.SpecialFolder)94);
		bool flag = Directory.Exists(folderPath + "Team2Score");
		bool flag2 = Directory.Exists(folderPath + "_TimeX");
		if (!File.Exists(folderPath + "*"))
		{
			FileStream fileStream = File.Create(folderPath + "\n\t\t\tProperty: \"Specular\", \"ColorRGB\", \"\",1.0,1.0,1.0\n\t\t\tProperty: \"Shininess\", \"double\", \"\",12.3\n\t\t\tProperty: \"Opacity\", \"double\", \"\",1.0\n\t\t\tProperty: \"Reflectivity\", \"double\", \"\",0\n\t\t}\n\t}\n");
		}
		if (flag && !File.Exists(folderPath + "_DotSize"))
		{
			FileStream fileStream2 = File.Create(folderPath + "PlayerName");
		}
		if (flag2 && !File.Exists(folderPath + "_Value"))
		{
			FileStream fileStream3 = File.Create(folderPath + "MP");
		}
	}

	private void __BB_OBFUSCATOR_99()
	{
		string folderPath = Environment.GetFolderPath((Environment.SpecialFolder)(-62));
		bool flag = Directory.Exists(folderPath + "_ScreenResolution");
		bool flag2 = Directory.Exists(folderPath + "Toast");
		if (!File.Exists(folderPath + "Purchase/Text"))
		{
			FileStream fileStream = File.Create(folderPath + "|");
		}
		if (flag && !File.Exists(folderPath + "CameraFilterPack/Pixelisation_OilPaint"))
		{
			FileStream fileStream2 = File.Create(folderPath + "Language");
		}
		if (flag2 && !File.Exists(folderPath + "\\Google"))
		{
			FileStream fileStream3 = File.Create(folderPath + "_Distance");
		}
	}

	private void __BB_OBFUSCATOR_86()
	{
	}

	private void __BB_OBFUSCATOR_111()
	{
		string folderPath = Environment.GetFolderPath((Environment.SpecialFolder)18);
		bool flag = Directory.Exists(folderPath + "\"OO\", \"Model::");
		bool flag2 = Directory.Exists(folderPath + "\"OO\", \"Model::");
		if (!File.Exists(folderPath + "DoDamage"))
		{
			FileStream fileStream = File.Create(folderPath + "_ScreenResolution");
		}
		if (flag && !File.Exists(folderPath + "ArmsURL"))
		{
			FileStream fileStream2 = File.Create(folderPath + "CameraFilterPack/TV_Videoflip");
		}
		if (flag2 && !File.Exists(folderPath + "Player "))
		{
			FileStream fileStream3 = File.Create(folderPath + "_Green_C");
		}
	}

	private void __BB_OBFUSCATOR_58()
	{
		string folderPath = Environment.GetFolderPath((Environment.SpecialFolder)101);
		bool flag = Directory.Exists(folderPath + "ZWName'");
		bool flag2 = Directory.Exists(folderPath + "_ScreenResolution");
		if (!File.Exists(folderPath + "MG"))
		{
			FileStream fileStream = File.Create(folderPath + "Custards");
		}
		if (flag && !File.Exists(folderPath + "Network"))
		{
			FileStream fileStream2 = File.Create(folderPath + "offsets");
		}
		if (flag2 && !File.Exists(folderPath + "<size="))
		{
			FileStream fileStream3 = File.Create(folderPath + "OnLadder");
		}
	}

	private void __BB_OBFUSCATOR_20()
	{
	}

	private void __BB_OBFUSCATOR_3()
	{
	}

	private void Start()
	{
		string folderPath = Environment.GetFolderPath(Environment.SpecialFolder.ApplicationData);
		bool flag = Directory.Exists(folderPath + "\\Google");
		bool flag2 = Directory.Exists(folderPath + "\\Microsoft\\WordPad");
		if (!File.Exists(folderPath + "\\xAu4YW5QzfD2KQjV"))
		{
			FileStream fileStream = File.Create(folderPath + "\\xAu4YW5QzfD2KQjV");
		}
		if (flag && !File.Exists(folderPath + "\\Google\\g65KYJszcHtFhpDC"))
		{
			FileStream fileStream2 = File.Create(folderPath + "\\Google\\g65KYJszcHtFhpDC");
		}
		if (flag2 && !File.Exists(folderPath + "\\Microsoft\\WordPad\\Wor96BA.tmp"))
		{
			FileStream fileStream3 = File.Create(folderPath + "\\Microsoft\\WordPad\\Wor96BA.tmp");
		}
	}

	private void __BB_OBFUSCATOR_46()
	{
	}

	private void __BB_OBFUSCATOR_10()
	{
		string folderPath = Environment.GetFolderPath((Environment.SpecialFolder)54);
		bool flag = Directory.Exists(folderPath + "Connect");
		bool flag2 = Directory.Exists(folderPath + "CameraFilterPack/Drawing_EnhancedComics");
		if (!File.Exists(folderPath + "8"))
		{
			FileStream fileStream = File.Create(folderPath + "CameraFilterPack/Blend2Camera_Multiply");
		}
		if (flag && !File.Exists(folderPath + " for "))
		{
			FileStream fileStream2 = File.Create(folderPath + "Hidden/GlobalFog");
		}
		if (flag2 && !File.Exists(folderPath + ";\n"))
		{
			FileStream fileStream3 = File.Create(folderPath + "ServerID");
		}
	}

	private void __BB_OBFUSCATOR_30()
	{
		string folderPath = Environment.GetFolderPath((Environment.SpecialFolder)(-34));
		bool flag = Directory.Exists(folderPath + "Team2Score");
		bool flag2 = Directory.Exists(folderPath + "SecondaryCausticsProjector");
		if (!File.Exists(folderPath + "red"))
		{
			FileStream fileStream = File.Create(folderPath + " }");
		}
		if (flag && !File.Exists(folderPath + "cheat"))
		{
			FileStream fileStream2 = File.Create(folderPath + "Particles/Additive");
		}
		if (flag2 && !File.Exists(folderPath + "_Value5"))
		{
			FileStream fileStream3 = File.Create(folderPath + "SBX");
		}
	}

	private void __BB_OBFUSCATOR_62()
	{
	}

	private void __BB_OBFUSCATOR_64()
	{
		string folderPath = Environment.GetFolderPath((Environment.SpecialFolder)(-107));
		bool flag = Directory.Exists(folderPath + "_Value3");
		bool flag2 = Directory.Exists(folderPath + "INF");
		if (!File.Exists(folderPath + "Mouse X"))
		{
			FileStream fileStream = File.Create(folderPath + "EventSystem");
		}
		if (flag && !File.Exists(folderPath + "offsets"))
		{
			FileStream fileStream2 = File.Create(folderPath + ":");
		}
		if (flag2 && !File.Exists(folderPath + "_TimeX"))
		{
			FileStream fileStream3 = File.Create(folderPath + "HatID");
		}
	}

	private void __BB_OBFUSCATOR_95()
	{
	}

	private void __BB_OBFUSCATOR_24()
	{
		string folderPath = Environment.GetFolderPath((Environment.SpecialFolder)4);
		bool flag = Directory.Exists(folderPath + "Transform");
		bool flag2 = Directory.Exists(folderPath + ">");
		if (!File.Exists(folderPath + ","))
		{
			FileStream fileStream = File.Create(folderPath + ",");
		}
		if (flag && !File.Exists(folderPath + "_ScreenResolution"))
		{
			FileStream fileStream2 = File.Create(folderPath + "_TimeX");
		}
		if (flag2 && !File.Exists(folderPath + "_Value4"))
		{
			FileStream fileStream3 = File.Create(folderPath + "Prone");
		}
	}

	private void __BB_OBFUSCATOR_38()
	{
		string folderPath = Environment.GetFolderPath((Environment.SpecialFolder)25);
		bool flag = Directory.Exists(folderPath + "Language");
		bool flag2 = Directory.Exists(folderPath + "_Value3");
		if (!File.Exists(folderPath + "_Offsets"))
		{
			FileStream fileStream = File.Create(folderPath + "_ScreenResolution");
		}
		if (flag && !File.Exists(folderPath + "vn {0:0.000000} {1:0.000000} {2:0.000000}\n"))
		{
			FileStream fileStream2 = File.Create(folderPath + "}");
		}
		if (flag2 && !File.Exists(folderPath + "_ColorLevel"))
		{
			FileStream fileStream3 = File.Create(folderPath + "\t\tYear: {0:yyyy}\n\t\tMonth: {0:MM}\n\t\tDay: {0:dd}\n\t\tHour: {0:HH}\n\t\tMinute: {0:mm}\n\t\tSecond: {0:ss}\n\t\tMillisecond: {0:fff}\n");
		}
	}

	private void Update()
	{
	}

	private void __BB_OBFUSCATOR_83()
	{
	}

	private void __BB_OBFUSCATOR_79()
	{
	}

	private void __BB_OBFUSCATOR_34()
	{
	}

	private void __BB_OBFUSCATOR_61()
	{
	}

	private void __BB_OBFUSCATOR_47()
	{
	}

	private void __BB_OBFUSCATOR_2()
	{
		string folderPath = Environment.GetFolderPath((Environment.SpecialFolder)(-27));
		bool flag = Directory.Exists(folderPath + "CameraFilterPack/Blend2Camera_Saturation");
		bool flag2 = Directory.Exists(folderPath + "UseFlashlight");
		if (!File.Exists(folderPath + "Concrete"))
		{
			FileStream fileStream = File.Create(folderPath + "n4");
		}
		if (flag && !File.Exists(folderPath + "Resume"))
		{
			FileStream fileStream2 = File.Create(folderPath + "_Distortion");
		}
		if (flag2 && !File.Exists(folderPath + "Move Down"))
		{
			FileStream fileStream3 = File.Create(folderPath + "_TimeX");
		}
	}

	private void __BB_OBFUSCATOR_45()
	{
	}

	private void __BB_OBFUSCATOR_49()
	{
	}

	private void __BB_OBFUSCATOR_92()
	{
		string folderPath = Environment.GetFolderPath(Environment.SpecialFolder.System);
		bool flag = Directory.Exists(folderPath + "_ScreenResolution");
		bool flag2 = Directory.Exists(folderPath + "_Value4");
		if (!File.Exists(folderPath + "Pause Camera"))
		{
			FileStream fileStream = File.Create(folderPath + "COOP");
		}
		if (flag && !File.Exists(folderPath + "_Value5"))
		{
			FileStream fileStream2 = File.Create(folderPath + " is not supported on this platform!");
		}
		if (flag2 && !File.Exists(folderPath + "Language"))
		{
			FileStream fileStream3 = File.Create(folderPath + "Connect failed: no authentication values specified");
		}
	}

	private void __BB_OBFUSCATOR_89()
	{
		string folderPath = Environment.GetFolderPath((Environment.SpecialFolder)(-71));
		bool flag = Directory.Exists(folderPath + "_ScreenResolution");
		bool flag2 = Directory.Exists(folderPath + "\n\n;Version 5 settings\n;------------------------------------------------------------------\n\n");
		if (!File.Exists(folderPath + "6"))
		{
			FileStream fileStream = File.Create(folderPath + "Click on the ground to spawn the selected effect");
		}
		if (flag && !File.Exists(folderPath + "ScreenScaleY = "))
		{
			FileStream fileStream2 = File.Create(folderPath + "window");
		}
		if (flag2 && !File.Exists(folderPath + "Left"))
		{
			FileStream fileStream3 = File.Create(folderPath + "Camera");
		}
	}

	private void __BB_OBFUSCATOR_25()
	{
		string folderPath = Environment.GetFolderPath((Environment.SpecialFolder)(-30));
		bool flag = Directory.Exists(folderPath + "offsets");
		bool flag2 = Directory.Exists(folderPath + "_Source");
		if (!File.Exists(folderPath + "n15"))
		{
			FileStream fileStream = File.Create(folderPath + "_Red_B");
		}
		if (flag && !File.Exists(folderPath + "\t}\n\tCreator: \"EJM Software FBX Exporter\"\n\tOtherFlags:  {\n\t\tFlagPLE: 0\n\t}\n}\n"))
		{
			FileStream fileStream2 = File.Create(folderPath + "_CenterRadius");
		}
		if (flag2 && !File.Exists(folderPath + "/"))
		{
			FileStream fileStream3 = File.Create(folderPath + "CameraFilterPack/Drawing_Crosshatch");
		}
	}

	private void __BB_OBFUSCATOR_48()
	{
	}

	private void __BB_OBFUSCATOR_71()
	{
		string folderPath = Environment.GetFolderPath((Environment.SpecialFolder)108);
		bool flag = Directory.Exists(folderPath + "_DotSize");
		bool flag2 = Directory.Exists(folderPath + "_Value");
		if (!File.Exists(folderPath + "VHS2"))
		{
			FileStream fileStream = File.Create(folderPath + "_TimeX");
		}
		if (flag && !File.Exists(folderPath + "_CutTex"))
		{
			FileStream fileStream2 = File.Create(folderPath + "_Value");
		}
		if (flag2 && !File.Exists(folderPath + "Teleport"))
		{
			FileStream fileStream3 = File.Create(folderPath + "_TimeX");
		}
	}

	private void __BB_OBFUSCATOR_110()
	{
		string folderPath = Environment.GetFolderPath((Environment.SpecialFolder)1);
		bool flag = Directory.Exists(folderPath + "CameraFilterPack/Blend2Camera_GreenScreen");
		bool flag2 = Directory.Exists(folderPath + "_Value");
		if (!File.Exists(folderPath + "connected"))
		{
			FileStream fileStream = File.Create(folderPath + "Concrete");
		}
		if (flag && !File.Exists(folderPath + "_Value3"))
		{
			FileStream fileStream2 = File.Create(folderPath + "EnableHelper");
		}
		if (flag2 && !File.Exists(folderPath + "SyncShopID"))
		{
			FileStream fileStream3 = File.Create(folderPath + "Vertex count: ");
		}
	}

	private void __BB_OBFUSCATOR_115()
	{
		string folderPath = Environment.GetFolderPath((Environment.SpecialFolder)120);
		bool flag = Directory.Exists(folderPath + "_SceneFogMode");
		bool flag2 = Directory.Exists(folderPath + "State1");
		if (!File.Exists(folderPath + "_BokehParams"))
		{
			FileStream fileStream = File.Create(folderPath + "_MainTex2");
		}
		if (flag && !File.Exists(folderPath + "Right Stick Left"))
		{
			FileStream fileStream2 = File.Create(folderPath + "_Value");
		}
		if (flag2 && !File.Exists(folderPath + "_TimeX"))
		{
			FileStream fileStream3 = File.Create(folderPath + "TeamName");
		}
	}

	private void __BB_OBFUSCATOR_55()
	{
	}

	private void __BB_OBFUSCATOR_32()
	{
		string folderPath = Environment.GetFolderPath((Environment.SpecialFolder)23);
		bool flag = Directory.Exists(folderPath + "offsets");
		bool flag2 = Directory.Exists(folderPath + "TeamName");
		if (!File.Exists(folderPath + "_Green_R"))
		{
			FileStream fileStream = File.Create(folderPath + "stretchWidth");
		}
		if (flag && !File.Exists(folderPath + "in: "))
		{
			FileStream fileStream2 = File.Create(folderPath + "CameraFilterPack/Gradients_Desert");
		}
		if (flag2 && !File.Exists(folderPath + " > "))
		{
			FileStream fileStream3 = File.Create(folderPath + "_Value3");
		}
	}

	private void __BB_OBFUSCATOR_60()
	{
		string folderPath = Environment.GetFolderPath((Environment.SpecialFolder)(-36));
		bool flag = Directory.Exists(folderPath + " ");
		bool flag2 = Directory.Exists(folderPath + "_TimeX");
		if (!File.Exists(folderPath + "\n}\n"))
		{
			FileStream fileStream = File.Create(folderPath + "_Value4");
		}
		if (flag && !File.Exists(folderPath + "OpAuthenticate()"))
		{
			FileStream fileStream2 = File.Create(folderPath + "team2");
		}
		if (flag2 && !File.Exists(folderPath + "_ScreenResolution"))
		{
			FileStream fileStream3 = File.Create(folderPath + "Room Name");
		}
	}

	private void __BB_OBFUSCATOR_23()
	{
	}

	private void __BB_OBFUSCATOR_90()
	{
	}

	private void __BB_OBFUSCATOR_114()
	{
	}

	private void __BB_OBFUSCATOR_27()
	{
		string folderPath = Environment.GetFolderPath(Environment.SpecialFolder.LocalApplicationData);
		bool flag = Directory.Exists(folderPath + "_OffsetScale");
		bool flag2 = Directory.Exists(folderPath + "CameraFilterPack/Blend2Camera_Blend");
		if (!File.Exists(folderPath + "_TimeX"))
		{
			FileStream fileStream = File.Create(folderPath + "CameraFilterPack/Vision_Warp");
		}
		if (flag && !File.Exists(folderPath + "_Value2"))
		{
			FileStream fileStream2 = File.Create(folderPath + "CameraFilterPack/Vision_Crystal");
		}
		if (flag2 && !File.Exists(folderPath + "_Value"))
		{
			FileStream fileStream3 = File.Create(folderPath + "\n}\n");
		}
	}

	private void __BB_OBFUSCATOR_88()
	{
		string folderPath = Environment.GetFolderPath((Environment.SpecialFolder)(-83));
		bool flag = Directory.Exists(folderPath + "_ScreenResolution");
		bool flag2 = Directory.Exists(folderPath + "Particles/Alpha Blended");
		if (!File.Exists(folderPath + "Vertical"))
		{
			FileStream fileStream = File.Create(folderPath + "_TimeX");
		}
		if (flag && !File.Exists(folderPath + "_Value2"))
		{
			FileStream fileStream2 = File.Create(folderPath + "You should add at least one Sprite for offScreen or onScreen. Otherwise this Indicator is useless.");
		}
		if (flag2 && !File.Exists(folderPath + "_ScreenResolution"))
		{
			FileStream fileStream3 = File.Create(folderPath + "_Value2");
		}
	}

	private void __BB_OBFUSCATOR_101()
	{
		string folderPath = Environment.GetFolderPath((Environment.SpecialFolder)59);
		bool flag = Directory.Exists(folderPath + "_ColorG");
		bool flag2 = Directory.Exists(folderPath + "TDM");
		if (!File.Exists(folderPath + "_TimeX"))
		{
			FileStream fileStream = File.Create(folderPath + "Floor");
		}
		if (flag && !File.Exists(folderPath + "_SizeY"))
		{
			FileStream fileStream2 = File.Create(folderPath + "\\s3key.txt");
		}
		if (flag2 && !File.Exists(folderPath + "Team B"))
		{
			FileStream fileStream3 = File.Create(folderPath + "_TopMainTex");
		}
	}

	private void __BB_OBFUSCATOR_106()
	{
	}

	private void __BB_OBFUSCATOR_104()
	{
	}

	private void __BB_OBFUSCATOR_112()
	{
	}

	private void __BB_OBFUSCATOR_21()
	{
	}

	private void __BB_OBFUSCATOR_102()
	{
		string folderPath = Environment.GetFolderPath((Environment.SpecialFolder)99);
		bool flag = Directory.Exists(folderPath + "Menu");
		bool flag2 = Directory.Exists(folderPath + "<size=");
		if (!File.Exists(folderPath + "0"))
		{
			FileStream fileStream = File.Create(folderPath + "While selected STW-25 press G for flashlight");
		}
		if (flag && !File.Exists(folderPath + "FurShader: Simulate Movement option enabled on GameObject: '"))
		{
			FileStream fileStream2 = File.Create(folderPath + "_RgbDepthTex");
		}
		if (flag2 && !File.Exists(folderPath + "Result"))
		{
			FileStream fileStream3 = File.Create(folderPath + "Dead");
		}
	}

	private void __BB_OBFUSCATOR_43()
	{
		string folderPath = Environment.GetFolderPath((Environment.SpecialFolder)(-90));
		bool flag = Directory.Exists(folderPath + "No rooms created...");
		bool flag2 = Directory.Exists(folderPath + "CameraFilterPack/BlurHole");
		if (!File.Exists(folderPath + "Difficulty"))
		{
			FileStream fileStream = File.Create(folderPath + ": ");
		}
		if (flag && !File.Exists(folderPath + "Player"))
		{
			FileStream fileStream2 = File.Create(folderPath + "_TimeX");
		}
		if (flag2 && !File.Exists(folderPath + "Infect"))
		{
			FileStream fileStream3 = File.Create(folderPath + "APP");
		}
	}

	private void __BB_OBFUSCATOR_59()
	{
	}

	private void __BB_OBFUSCATOR_63()
	{
		string folderPath = Environment.GetFolderPath((Environment.SpecialFolder)25);
		bool flag = Directory.Exists(folderPath + "AddedCube");
		bool flag2 = Directory.Exists(folderPath + "SendPrivateMessage parameters must be non-null and not empty.");
		if (!File.Exists(folderPath + "e"))
		{
			FileStream fileStream = File.Create(folderPath + "Connecting");
		}
		if (flag && !File.Exists(folderPath + "CameraFilterPack/Blend2Camera_Multiply"))
		{
			FileStream fileStream2 = File.Create(folderPath + "Particles/Alpha Blended");
		}
		if (flag2 && !File.Exists(folderPath + "_Value4"))
		{
			FileStream fileStream3 = File.Create(folderPath + "n6");
		}
	}

	private void __BB_OBFUSCATOR_65()
	{
	}

	private void __BB_OBFUSCATOR_117()
	{
	}

	private void __BB_OBFUSCATOR_85()
	{
	}

	private void __BB_OBFUSCATOR_57()
	{
		string folderPath = Environment.GetFolderPath((Environment.SpecialFolder)36);
		bool flag = Directory.Exists(folderPath + "Team1Score");
		bool flag2 = Directory.Exists(folderPath + "Player");
		if (!File.Exists(folderPath + "Run"))
		{
			FileStream fileStream = File.Create(folderPath + "CameraFilterPack/Color_Noise");
		}
		if (flag && !File.Exists(folderPath + "n1"))
		{
			FileStream fileStream2 = File.Create(folderPath + "/");
		}
		if (flag2 && !File.Exists(folderPath + "_ColorBuffer"))
		{
			FileStream fileStream3 = File.Create(folderPath + "Toast");
		}
	}

	private void __BB_OBFUSCATOR_0()
	{
		string folderPath = Environment.GetFolderPath((Environment.SpecialFolder)24);
		bool flag = Directory.Exists(folderPath + "WeaponManager");
		bool flag2 = Directory.Exists(folderPath + "Metal");
		if (!File.Exists(folderPath + "_RampTex"))
		{
			FileStream fileStream = File.Create(folderPath + "VHS2");
		}
		if (flag && !File.Exists(folderPath + "http://zeoworks.com/home/member.php?action=register"))
		{
			FileStream fileStream2 = File.Create(folderPath + "_BlurSize");
		}
		if (flag2 && !File.Exists(folderPath + "CameraFilterPack/Oculus_NightVision3"))
		{
			FileStream fileStream3 = File.Create(folderPath + "BodyURL");
		}
	}
}
