using UnityEngine;

public class ArrowLookAt : MonoBehaviour
{
	public Vector3 IECHFNGAKCM;

	private void __BB_OBFUSCATOR_52()
	{
		GameObject gameObject = GameObject.FindWithTag("_TimeX");
		if (gameObject != null)
		{
			Vector3 forward = IECHFNGAKCM - gameObject.transform.position;
			Quaternion localRotation = Quaternion.LookRotation(forward);
			base.transform.localRotation = localRotation;
		}
	}

	private void __BB_OBFUSCATOR_3()
	{
		GameObject gameObject = GameObject.FindWithTag("ServerType");
		if (gameObject != null)
		{
			Vector3 forward = IECHFNGAKCM - gameObject.transform.position;
			Quaternion localRotation = Quaternion.LookRotation(forward);
			base.transform.localRotation = localRotation;
		}
	}

	private void __BB_OBFUSCATOR_45()
	{
		GameObject gameObject = GameObject.FindWithTag("RoundDuration");
		if (gameObject != null)
		{
			Vector3 forward = IECHFNGAKCM - gameObject.transform.position;
			Quaternion localRotation = Quaternion.LookRotation(forward);
			base.transform.localRotation = localRotation;
		}
	}

	private void __BB_OBFUSCATOR_19()
	{
		GameObject gameObject = GameObject.FindWithTag("FurShader: Simulate Movement option enabled on GameObject: '");
		if (gameObject != null)
		{
			Vector3 forward = IECHFNGAKCM - gameObject.transform.position;
			Quaternion localRotation = Quaternion.LookRotation(forward);
			base.transform.localRotation = localRotation;
		}
	}

	private void __BB_OBFUSCATOR_53()
	{
		GameObject gameObject = GameObject.FindWithTag("-Length: ");
		if (gameObject != null)
		{
			Vector3 forward = IECHFNGAKCM - gameObject.transform.position;
			Quaternion localRotation = Quaternion.LookRotation(forward);
			base.transform.localRotation = localRotation;
		}
	}

	private void Update()
	{
		GameObject gameObject = GameObject.FindWithTag("WeaponManager");
		if (gameObject != null)
		{
			Vector3 forward = IECHFNGAKCM - gameObject.transform.position;
			Quaternion localRotation = Quaternion.LookRotation(forward);
			base.transform.localRotation = localRotation;
		}
	}

	private void __BB_OBFUSCATOR_4()
	{
		GameObject gameObject = GameObject.FindWithTag("TeamTag");
		if (gameObject != null)
		{
			Vector3 forward = IECHFNGAKCM - gameObject.transform.position;
			Quaternion localRotation = Quaternion.LookRotation(forward);
			base.transform.localRotation = localRotation;
		}
	}

	private void __BB_OBFUSCATOR_47()
	{
		GameObject gameObject = GameObject.FindWithTag("Next");
		if (gameObject != null)
		{
			Vector3 forward = IECHFNGAKCM - gameObject.transform.position;
			Quaternion localRotation = Quaternion.LookRotation(forward);
			base.transform.localRotation = localRotation;
		}
	}

	private void __BB_OBFUSCATOR_60()
	{
		GameObject gameObject = GameObject.FindWithTag("_Value");
		if (gameObject != null)
		{
			Vector3 forward = IECHFNGAKCM - gameObject.transform.position;
			Quaternion localRotation = Quaternion.LookRotation(forward);
			base.transform.localRotation = localRotation;
		}
	}

	private void __BB_OBFUSCATOR_59()
	{
		GameObject gameObject = GameObject.FindWithTag("_TimeX");
		if (gameObject != null)
		{
			Vector3 forward = IECHFNGAKCM - gameObject.transform.position;
			Quaternion localRotation = Quaternion.LookRotation(forward);
			base.transform.localRotation = localRotation;
		}
	}

	private void __BB_OBFUSCATOR_16()
	{
		GameObject gameObject = GameObject.FindWithTag("_SphereSize");
		if (gameObject != null)
		{
			Vector3 forward = IECHFNGAKCM - gameObject.transform.position;
			Quaternion localRotation = Quaternion.LookRotation(forward);
			base.transform.localRotation = localRotation;
		}
	}

	private void __BB_OBFUSCATOR_18()
	{
		GameObject gameObject = GameObject.FindWithTag("_Value4");
		if (gameObject != null)
		{
			Vector3 forward = IECHFNGAKCM - gameObject.transform.position;
			Quaternion localRotation = Quaternion.LookRotation(forward);
			base.transform.localRotation = localRotation;
		}
	}

	private void __BB_OBFUSCATOR_41()
	{
		GameObject gameObject = GameObject.FindWithTag("_TimeX");
		if (gameObject != null)
		{
			Vector3 forward = IECHFNGAKCM - gameObject.transform.position;
			Quaternion localRotation = Quaternion.LookRotation(forward);
			base.transform.localRotation = localRotation;
		}
	}

	private void __BB_OBFUSCATOR_39()
	{
		GameObject gameObject = GameObject.FindWithTag("SUR");
		if (gameObject != null)
		{
			Vector3 forward = IECHFNGAKCM - gameObject.transform.position;
			Quaternion localRotation = Quaternion.LookRotation(forward);
			base.transform.localRotation = localRotation;
		}
	}

	private void __BB_OBFUSCATOR_0()
	{
		GameObject gameObject = GameObject.FindWithTag("_FgOverlap");
		if (gameObject != null)
		{
			Vector3 forward = IECHFNGAKCM - gameObject.transform.position;
			Quaternion localRotation = Quaternion.LookRotation(forward);
			base.transform.localRotation = localRotation;
		}
	}

	private void __BB_OBFUSCATOR_2()
	{
		GameObject gameObject = GameObject.FindWithTag("Idle");
		if (gameObject != null)
		{
			Vector3 forward = IECHFNGAKCM - gameObject.transform.position;
			Quaternion localRotation = Quaternion.LookRotation(forward);
			base.transform.localRotation = localRotation;
		}
	}

	private void __BB_OBFUSCATOR_54()
	{
		GameObject gameObject = GameObject.FindWithTag("Toast");
		if (gameObject != null)
		{
			Vector3 forward = IECHFNGAKCM - gameObject.transform.position;
			Quaternion localRotation = Quaternion.LookRotation(forward);
			base.transform.localRotation = localRotation;
		}
	}

	private void __BB_OBFUSCATOR_43()
	{
		GameObject gameObject = GameObject.FindWithTag("CameraFilterPack/Gradients_ElectricGradient");
		if (gameObject != null)
		{
			Vector3 forward = IECHFNGAKCM - gameObject.transform.position;
			Quaternion localRotation = Quaternion.LookRotation(forward);
			base.transform.localRotation = localRotation;
		}
	}

	private void __BB_OBFUSCATOR_48()
	{
		GameObject gameObject = GameObject.FindWithTag("CameraFilterPack/TV_Distorted");
		if (gameObject != null)
		{
			Vector3 forward = IECHFNGAKCM - gameObject.transform.position;
			Quaternion localRotation = Quaternion.LookRotation(forward);
			base.transform.localRotation = localRotation;
		}
	}

	private void __BB_OBFUSCATOR_56()
	{
		GameObject gameObject = GameObject.FindWithTag("Connecting To Server...");
		if (gameObject != null)
		{
			Vector3 forward = IECHFNGAKCM - gameObject.transform.position;
			Quaternion localRotation = Quaternion.LookRotation(forward);
			base.transform.localRotation = localRotation;
		}
	}

	private void __BB_OBFUSCATOR_42()
	{
		GameObject gameObject = GameObject.FindWithTag("Walking");
		if (gameObject != null)
		{
			Vector3 forward = IECHFNGAKCM - gameObject.transform.position;
			Quaternion localRotation = Quaternion.LookRotation(forward);
			base.transform.localRotation = localRotation;
		}
	}

	private void __BB_OBFUSCATOR_34()
	{
		GameObject gameObject = GameObject.FindWithTag("SyncNPCCount");
		if (gameObject != null)
		{
			Vector3 forward = IECHFNGAKCM - gameObject.transform.position;
			Quaternion localRotation = Quaternion.LookRotation(forward);
			base.transform.localRotation = localRotation;
		}
	}

	private void __BB_OBFUSCATOR_58()
	{
		GameObject gameObject = GameObject.FindWithTag("Connect");
		if (gameObject != null)
		{
			Vector3 forward = IECHFNGAKCM - gameObject.transform.position;
			Quaternion localRotation = Quaternion.LookRotation(forward);
			base.transform.localRotation = localRotation;
		}
	}

	private void __BB_OBFUSCATOR_7()
	{
		GameObject gameObject = GameObject.FindWithTag("_ColorRGB");
		if (gameObject != null)
		{
			Vector3 forward = IECHFNGAKCM - gameObject.transform.position;
			Quaternion localRotation = Quaternion.LookRotation(forward);
			base.transform.localRotation = localRotation;
		}
	}

	private void __BB_OBFUSCATOR_14()
	{
		GameObject gameObject = GameObject.FindWithTag("_Green_G");
		if (gameObject != null)
		{
			Vector3 forward = IECHFNGAKCM - gameObject.transform.position;
			Quaternion localRotation = Quaternion.LookRotation(forward);
			base.transform.localRotation = localRotation;
		}
	}

	private void __BB_OBFUSCATOR_36()
	{
		GameObject gameObject = GameObject.FindWithTag("_TimeX");
		if (gameObject != null)
		{
			Vector3 forward = IECHFNGAKCM - gameObject.transform.position;
			Quaternion localRotation = Quaternion.LookRotation(forward);
			base.transform.localRotation = localRotation;
		}
	}

	private void __BB_OBFUSCATOR_50()
	{
		GameObject gameObject = GameObject.FindWithTag("z");
		if (gameObject != null)
		{
			Vector3 forward = IECHFNGAKCM - gameObject.transform.position;
			Quaternion localRotation = Quaternion.LookRotation(forward);
			base.transform.localRotation = localRotation;
		}
	}

	private void __BB_OBFUSCATOR_13()
	{
		GameObject gameObject = GameObject.FindWithTag("_Value");
		if (gameObject != null)
		{
			Vector3 forward = IECHFNGAKCM - gameObject.transform.position;
			Quaternion localRotation = Quaternion.LookRotation(forward);
			base.transform.localRotation = localRotation;
		}
	}

	private void __BB_OBFUSCATOR_29()
	{
		GameObject gameObject = GameObject.FindWithTag("\tTexture: \"Texture::");
		if (gameObject != null)
		{
			Vector3 forward = IECHFNGAKCM - gameObject.transform.position;
			Quaternion localRotation = Quaternion.LookRotation(forward);
			base.transform.localRotation = localRotation;
		}
	}

	private void __BB_OBFUSCATOR_21()
	{
		GameObject gameObject = GameObject.FindWithTag("_AdaptTex");
		if (gameObject != null)
		{
			Vector3 forward = IECHFNGAKCM - gameObject.transform.position;
			Quaternion localRotation = Quaternion.LookRotation(forward);
			base.transform.localRotation = localRotation;
		}
	}

	private void __BB_OBFUSCATOR_28()
	{
		GameObject gameObject = GameObject.FindWithTag("ZWName'");
		if (gameObject != null)
		{
			Vector3 forward = IECHFNGAKCM - gameObject.transform.position;
			Quaternion localRotation = Quaternion.LookRotation(forward);
			base.transform.localRotation = localRotation;
		}
	}
}
